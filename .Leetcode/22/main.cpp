#include <bits/stdc++.h>
#define FI "input.INP"
using namespace	std;

void test(bool check){
	if(check)
		freopen(FI, "r", stdin);
}

vector<string> generateParenthesis(int n) {
	vector<string> res;
	string str;

	

	return res;
}

void func(){
	int n;
	cin >> n;
	generateParenthesis(n);
}

int main(){
	test(true);
	func();
	return 0;
}